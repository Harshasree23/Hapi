<link href="https://fonts.googleapis.com/css2?family=Nova+Mono&display=swap" rel="stylesheet">
<link rel="icon" href="/public/favicon.png">
<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
<link href="https://fonts.googleapis.com/css2?family=Urbanist:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet">
<style>
    * {
        box-sizing: border-box;
        margin: 0;
        padding: 0;
        font-family: "Urbanist", serif;
        color: #333;
    }

    body {
        background-color: #f4f4f4;
        overflow-x: hidden;
    }

    .hover {
        background-color: rgba(173, 216, 230, 0.2); 
        text-decoration: none;
        padding: 1rem;
        border-radius: 0.3rem;
        color: #333;
        border: 0.1rem solid #A0C8E4;
        transition: background-color 0.3s ease, transform 0.3s ease;
    }

    .hover:hover {
        background-color: #A0C8E4; 
        transform: scale(1.05);
        box-shadow: 0.4rem 0.4rem 1rem rgba(160, 200, 228, 0.5);
    }

    .goto {
        padding: 3rem;
        margin: 3rem auto;
        width: fit-content;
        background-color: rgba(173, 216, 230, 0.2);
        border-radius: 0.3rem;
        display: grid;
        gap: 2rem;
        color: #333;
        border: 0.1rem solid #A0C8E4;
    }

    .goto a {
        text-decoration: none;
        color: #333;
        background-color: rgba(173, 216, 230, 0.2);
        font-size: 1.5rem;
        width: fit-content;
        padding: 1rem;
        border-radius: 0.3rem;
        display: flex;
        gap: 1rem;
        justify-content: center;
        align-items: center;
        border: 0.1rem solid #A0C8E4;
        transition: background-color 0.3s ease, transform 0.3s ease;
    }

    .goto a:hover {
        background-color: #A0C8E4; 
        transform: scale(1.05);
        box-shadow: 0.4rem 0.4rem 1rem rgba(160, 200, 228, 0.5);
    }

    .main-heading {
        min-height: 15vh;
        padding: 2rem;
        font-size: 1.5rem;
        display: flex;
        justify-content: space-around;
        align-items: center;
        background-color: rgba(173, 216, 230, 0.2);
        border-bottom: 0.2rem solid #A0C8E4;
        color: #333;
        font-family: "Nova Mono", monospace;
        border-radius: 0 0 1rem 1rem;
    }

    .main-heading button {
        font-size: 1.5rem;
    }

    form {
        width: 80%;
        background-color: rgba(173, 216, 230, 0.1);
        display: grid;
        padding: 2rem;
        margin: 2rem auto;
        font-size: 1.2rem;
        border-radius: 0.3rem;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        color: #333;
        border: 0.1rem solid #A0C8E4;
    }

    input {
        padding: 0.5rem;
        font-size: 1.2rem;
        width: 100%;
        margin-bottom: 1.5rem;
        border: 0.1rem solid #A0C8E4;
        border-radius: 0.3rem;
        background-color: rgba(173, 216, 230, 0.05);
        color: #333;
    }

    label {
        font-size: 1.2rem;
        padding: 0.3rem;
        color: #333;
    }

    button {
        padding: 0.7rem;
        border: none;
        border-radius: 0.3rem;
        background-color: #58A6D8;
        color: #fff;
        font-size: 1.2rem;
        cursor: pointer;
        transition: background-color 0.3s ease, transform 0.3s ease;
    }

    button:hover {
        background-color: #4C8FBB;
        transform: translateY(-3px);
    }

    form button {
        margin: 1rem auto;
        width: fit-content;
    }

    @keyframes fadeIn {
        from {
            opacity: 0;
        }
        to {
            opacity: 1;
        }
    }

    @media (max-width: 850px) {
        .main-heading {
            flex-wrap: wrap;
            justify-content: left;
            align-items: flex-start;
            gap: 1rem;
            font-size: 1.5rem;
        }

        form {
            padding: 1.5rem;
        }

        .goto {
            width: 75%;
            padding: 1rem;
        }

        .goto a {
            font-size: 1rem;
        }
    }

    @media (max-width: 576px) {
        .goto {
            width: 90%;
        }
    }
</style>
